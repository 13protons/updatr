/**
  Validate the response object from the update server
  @param {object} json The JSON encoded response from the update server
  @return {array} A list of recent releases
  */
export default function(json) {
  // do some checks
  return json;
}
